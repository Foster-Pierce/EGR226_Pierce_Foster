#include "msp.h"
#include "LCD_preset.h"

void main(void)
{
    WDT_A->CTL = WDT_A_CTL_PW | WDT_A_CTL_HOLD;		// stop watchdog timer

    char FN[] = {'P','I','E','R','C','E'};
    char LN[] = {'F','O','S','T','E','R'};
    char LN[] = {'E','G','R'};
    char LN[] = {'2','2','6'};

    LCD_init();
    while(1){
        ;
    }
}
